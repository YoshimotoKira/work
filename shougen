#弧度法での角度tが与えられた時の点(cost,sint)の象限を出力するプログラム
import math

t = float(input("実数を入力してください。（単位はrad）"))

if (math.sin(t) > 0) and (math.cos(t) > 0):
  print("第一象限")
elif (math.sin(t) > 0) and (math.cos(t) < 0):
  print("第二象限")
elif (math.sin(t) < 0) and (math.cos(t) < 0):
  print("第三象限")
else: print("第四象限")
